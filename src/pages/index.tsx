import GlobalStyles from '../styles/Global';

// @TODO: Protect page/Redirect to login if not session

const Home = () => (
  <GlobalStyles>
    <h1>Welcome to Wiser</h1>
  </GlobalStyles>
);

export default Home;
